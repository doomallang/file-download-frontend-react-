import { createGlobalStyle } from "styled-components"

const BackgroundColor = createGlobalStyle`
    body {
        background-color:rgba(35, 37, 41, 0.09);
    }
`

export default BackgroundColor